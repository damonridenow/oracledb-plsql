{
  "name": "oracledb-plsql",
  "version": "2.0.2",
  "description": "use plsql package directly from api calls",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Daniele Sergi",
  "license": "ISC",
  "dependencies": {
    "oracledb": "^5.3.0"
  }
}
